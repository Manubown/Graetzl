// localStorage key convention: `graetzl:<feature>` — keeps all keys in one auditable place.
export const STORAGE_KEYS = {
  THEME: "graetzl:theme",
  WELCOME_DISMISSED: "graetzl:welcome-dismissed-v2",
} as const;
